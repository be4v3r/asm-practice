# Практична 3. Ввід числа і базові операції з регістрами та пам’яттю

**Мова:** Assembler i386 (NASM)  
**ОС:** Debian Linux  
**Ввід/вивід:** лише `int 0x80` (sys_read/sys_write/sys_exit)

## Мета
Навчитися читати рядок з консолі, перетворювати на число, зберігати в пам’яті та виконувати прості арифметичні операції.

## Вхідні дані
Одне ціле число x (0..1_000_000), введене з консолі у вигляді десяткового рядка з '\n'.

## Вихідні дані
Три рядки:
1) x
2) x + A
3) x - B

## Що потрібно використати
- sys_read/sys_write/sys_exit через int 80h
- власні підпрограми atoi та itoa
- змінна x у .bss (операції з пам’яттю)
- call/ret для підпрограм

## Персоналізація (обов’язково)
Обчислити:
- A = 10 + D
- B = 20 + M
де D — день народження (1..31), M — місяць (1..12).

### Позначення персональних параметрів
- `Y3` — третя цифра року народження (наприклад 2004 → 0)
- `Y4` — четверта цифра року народження (2004 → 4)
- `D` — день народження (1..31)
- `M` — місяць народження (1..12)
- `N` — порядковий номер літери **'n'** у прізвищі (1..len); якщо 'n' немає — `N = 3`
- `K = 1 + (Y3 + Y4) mod 9`
- `S = D + 10*M + 100*Y4`

## Критерії до коду
- Код компілюється NASM та лінкується `ld` (команди нижче).
- У коді є коментарі базових блоків: `I/O`, `parse`, `math`, `logic`, `loops`, `memory`.
- Немає коментарів, що згадують ChatGPT.
- Результат відповідає вимогам задачі та коректно обробляє описані граничні випадки.

## Збірка і запуск
```bash
./compile.sh
./link.sh
./practice3
```

## Доказ виконання
У `screenshot.png` має бути видно:
1) запуск `./compile.sh`
2) запуск `./link.sh`
3) запуск програми та її вивід
